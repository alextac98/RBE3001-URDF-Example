

robot = makeRobot('rbe3001arm.urdf');
robot_cfg = homeCon
dispRobot(robot);

wait(1);


function robot=makeRobot(urdf_loc)
    robot = importrobot(urdf_loc);
end

function moveRobot(robot_config)
    
end

function dispRobot(robot)
    show(robot);
    axis([-0.25, 0.4, -0.2, 0.2, -0.05, 0.4]);
    axis off
end